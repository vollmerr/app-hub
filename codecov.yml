coverage:
  precision: 2
  round: down
  range: 70...100

  status:
    project:
      target: 70%
    patch: false
    changes: false

comment:
  layout: "header, diff"
  behavior: default  # update if exists else create new
