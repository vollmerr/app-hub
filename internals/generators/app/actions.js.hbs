import { createAction } from 'redux-actions';

import {
  INIT_DATA_REQUEST,
  INIT_DATA_SUCCESS,
  INIT_DATA_FAILURE,
} from './constants';

export const initRequest = createAction(INIT_DATA_REQUEST);
export const initSuccess = createAction(INIT_DATA_SUCCESS);
export const initFailure = createAction(INIT_DATA_FAILURE);
