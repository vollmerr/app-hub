/**
 * Test initializeIcons
 */

import initializeIcons from '../initializeIcons';

describe('initializeIcons', () => {
  it('should not throw an error', () => {
    expect(() => initializeIcons()).not.toThrow();
  });
});
